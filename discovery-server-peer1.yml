eureka:
  instance:
    hostname: peer2
    metadataMap:
      zone: zone1
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/,http://localhost:8890/eureka/
server:
  port: ${PORT:8888}