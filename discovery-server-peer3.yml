eureka:
  instance:
    hostname: peer2
    metadataMap:
      zone: zone3
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/,http://localhost:8888/eureka/
server:
  port: ${PORT:8890}